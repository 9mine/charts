apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "9p-execfuse-jinja2.fullname" . }}-test-connection"
  labels:
    {{- include "9p-execfuse-jinja2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "9p-execfuse-jinja2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
