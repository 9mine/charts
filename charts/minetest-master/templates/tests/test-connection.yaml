apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minetest-master.fullname" . }}-test-connection"
  labels:
    {{- include "minetest-master.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minetest-master.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
