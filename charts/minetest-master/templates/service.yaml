apiVersion: v1
kind: Service
metadata:
  name: {{ include "minetest-master.fullname" . }}
  labels:
    {{- include "minetest-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minetest-master.selectorLabels" . | nindent 4 }}
